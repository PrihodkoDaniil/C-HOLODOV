1.Какие элементы управления используются для создания главного меню 
приложения?Menustrip
2. Какие элементы управления используются для создания контекстного 
меню элемента управления?ContextMenuStrip
3. Как в режиме разработки указать элементу управления его контекстное 
меню? в сойствах будет Context там и указать
4. Какие типы данных используются для создания пунктов меню? textbox,label,comboBox=>string
5. Поясните механизм синхронизации событий от разных элементов управления.
При добавлении/изменении в одном элементе то же мгоновенно происходит и в другом
6. Поясните назначение конструкции try … catch …(если выполняется условие то try если нет то catch)
7. Какие классы используются для создания строки состояния в .NET? StatusStrip
8. Какие классы используются для создания панелей строки состояния? Statuslabel,progressbar,dropdownbutton
splitbutton
Каково назначение каждого из этих классов?метка,индикатор прогресса,два выпадающих списка
9. Какой класс используется для работы с таймером? Какое событие элемента-таймера необходимо обрабатывать 
для реагирования на смену системного времени? Timer DateTime
10. С помощью какого класса можно получить текущие дату и время? Какие 
методы содержит данный класс? DateTime
11. Что позволяет настраивать свойство Spring элементов-панелей строки 
состояния?
